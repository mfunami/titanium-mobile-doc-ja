#summary API案内 - メディア編 - 動画再生・音声再生
#labels api,media

= API案内 - メディア編 - 動画再生・音声再生 =

参考文献：[http://www.codestrong.com/timobile/guides/rich_media_api/ 
Titanium Mobile Rich Media API Guide]

== 動画ストリーミング再生 ==
Titaniumではアプリケーション内動画再生ができます。使いどころが難しいような気がしないでもありませんが(^^;
{{{
var video = Titanium.Media.createVideoPlayer({
    contentURL : "movie.mp4"
});
video.addEventListener("complete", function() {
    video.removeEventListener('complete', listenerId);
    var dialog = Titanium.UI.createAlertDialog({
        'title' : '再生終了',
        'message' : 'video completed',
        'buttonNames' : [ 'OK' ]
    });
    dialog.show();
});
video.play();
}}}
== 音声ストリーミング再生 == 
ループ再生とかボリュームコントロールなどもAPIもありますが、とりあえず再生機能のサンプルをどうぞ。
{{{
var sound = Titanium.Media.createSound('cricket.wav');
sound.addEventListener('complete', function() {
    sound.removeEventListener('complete', listenerId);
    var dialog = Titanium.UI.createAlertDialog({
        'title' : 'Sound Complete',
        'message' : 'sound completed',
        'buttonNames' : [ 'OK' ]
    });
    dialog.show();
});
sound.play();
}}}