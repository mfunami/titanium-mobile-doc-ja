#summary APIカタログ(ユーティリティ編) - タイマー処理
#labels api,embeded
= APIカタログ(ユーティリティ編) - タイマー処理 =
Titaniumには!JavaScriptの{{{setTimeout}}}と{{{setInterval}}}を使って、タイマー処理をします。

{{{setTimeout}}}は指定した時間後に処理を実行させるのに対し、{{{setInterval}}}は一定間隔で処理を繰り返し実行します。
{{{
//setTimeoutの例
var labelTimeout = Ti.UI.createLabel({
    text: 'setTimeout',
    textAlign:'center',
    width:'auto',
    height: 20,
    top:20
});
Ti.UI.currentWindow.add(labelTimeout);

// 3000ms後に描画する。
setTimeout(function(){
	labelTimeout.text = "3 sec timer fired!!";
}, 3000);
	
// setIntervalの例
var count = 0;
var labelInterval = Ti.UI.createLabel({
    text: '0',
    textAlign:'center',
    width:'auto',
    height: 20,
    top:100
});
Ti.UI.currentWindow.add(labelInterval);

// 10msごとにカウントアップし、描画する。
setInterval(function(){
    count++;
    labelInterval.text = "Interval fired " + count;
}, 10);
}}}

== ロック時のタイマー処理 ==
スクリーンロック時にTimerを制御する Titanium.App.idleTimerDisabled というプロパティがあるのですが、どうもTitanium 1.0.0では正しく動作していません。仕様上はtrueにするとtimerを無効にできるのですが...

== 関連するAPIドキュメント ==
 * https://developer.appcelerator.com/apidoc/mobile/latest/Titanium.App
----
原著作者表示：[http://www.appcelerator.com/ Appcelerator, Inc]